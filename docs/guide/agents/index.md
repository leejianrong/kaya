# Agents & MCP

Placeholder page. Wiring kaya into an agent over MCP, the tools it exposes, and the shared
`kaya-client` payload shaping behind both the CLI and the MCP server will be documented here
(KAN-1196).
